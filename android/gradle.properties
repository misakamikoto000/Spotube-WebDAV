org.gradle.jvmargs=-Xmx4608m -Dfile.encoding=UTF-8 -XX:+UseParallelGC
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.enableJetifier=true
# Flutter plugins are resolved from the C: pub cache while this project lives
# on D:. Kotlin's incremental cache cannot relativize sources across drives.
kotlin.incremental=false
